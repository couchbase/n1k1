-- label: ET-30002
-- description: unfixtured WARN query
-- source: logs
-- A recipe WITHOUT a fixture: .rules test counts it as "no fixture" -- it is NOT a
-- hard failure (only a fixture WITHOUT an @expect fails). Add a fixture to make it
-- testable.
SELECT * FROM logs l WHERE l.sev = "WARN"
