-- ticket: ET-30002
-- severity: low
-- source: logs
-- A recipe WITHOUT a fixture: .detect test counts it as "no fixture" -- it is NOT a
-- hard failure (only a fixture WITHOUT an @expect fails). Add a fixture to make it
-- testable.
SELECT * FROM logs l WHERE l.sev = "WARN"
