---
type: table
title: Legacy import
tags: [archive, unmigrated
owner: data-eng
---

The frontmatter above is **intentionally invalid YAML** (the `tags` list is
never closed). n1k1 does not fail the scan for it: the row carries
`front_error` instead, so a corpus-hygiene sweep is a query rather than a
silent omission.

# Status

Superseded by [orders](/concepts/orders).
