# Scratch notes

Deliberately has **no frontmatter**, so `front` is MISSING for this row — the
"which concepts are unregistered?" query below is what finds it.

## Todo

- give this a `type` and an owner
- or delete it
